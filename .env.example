# Copy this file to `.env` and fill in the values as needed.
# Values here are safe defaults for local development.

# SECURITY
SECRET_KEY=
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
CSRF_TRUSTED_ORIGINS=http://127.0.0.1,http://localhost

# Database (LOCAL - use Postgres on localhost instead of SQLite)
# Adjust user, password and DB name as per your local installation
DATABASE_URL=postgres://postgres:postgres@localhost:5432/estoque_db

# Optional SQLite overrides
# FORCE_SQLITE=1 forces Django to ignore DATABASE_URL and use SQLite.
# SQLITE_PATH can point to a persistent Railway volume path, for example:
# SQLITE_PATH=/app/data/db.sqlite3

# Production (Railway) – set these in Railway Dashboard Environment Variables
# SECRET_KEY=your-strong-secret-key
# DEBUG=False
# ALLOWED_HOSTS=.up.railway.app,yourdomain.com,www.yourdomain.com
# CSRF_TRUSTED_ORIGINS=https://your-app.up.railway.app,https://yourdomain.com,https://www.yourdomain.com
